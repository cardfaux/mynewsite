import React from "react"

import Layout from "../components/Shared/Layout"

const error = () => {
  return (
    <Layout>
      <header>
        <h1>OOPS it's a dead end back to home page</h1>
      </header>
    </Layout>
  )
}

export default error
